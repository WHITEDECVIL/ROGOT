version: '3.8'
services:
  server:
    build: ./server
    ports:
      - "4000:4000"
    environment:
      - CLAUDE_API_KEY=${CLAUDE_API_KEY}
    volumes:
      - ./server/data:/app/data
    restart: unless-stopped